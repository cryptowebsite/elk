---
all:
  hosts:
    el-centos:
      ansible_host: localhost
    kbn-centos:
      ansible_host: localhost
    fb-centos:
      ansible_host: localhost

  # vars:
  #   ansible_connection: ssh
  #   ansible_user: netology

elasticsearch:
  hosts:
    el-centos:
kibana:
  hosts:
    kbn-centos:
filebeat:
  hosts:
    fb-centos:
